name: iot_models
description: Common models and converters for iot websocket server and client.
version: 1.1.16

environment:
  sdk: '>=3.0.0 <3.5.0'

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  freezed: ^2.4.2
  json_serializable: ^6.7.1
  build_runner: ^2.4.6
  lints: ^2.1.1
